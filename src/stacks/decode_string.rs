pub fn decode_strings(s: String) -> String {
    let string_stack: Vec<String> = Vec::new();
    let number_stack: Vec<i32> = Vec::new();
    let current_string = String::new();
    let current_number = 0;

    current_string
}
